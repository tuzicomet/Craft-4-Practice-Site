//PostCSS configuration file

module.exports = {
    // plugins to process CSS with
    plugins: {
        tailwindcss: {},  // adds tailwind CSS utilities
        autoprefixer: {}, // adds vendor prefixes for better browser support
    },
};